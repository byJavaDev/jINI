; These entries are all in the default section

framework=Jini
version=1.0
release=true

; Now we will create another section
[author]
alias=byJavaDev

; And another one...
[description]
header=JIni - Parse .ini files in Java
description=This small library allows you to parse ini files in Java!

; Let's see what happens if we use the same key twice - but in another section (!)
version=1.0

; That's it! :)